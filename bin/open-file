#!/usr/bin/env sh
case "$1" in
	*.webm | *.mp4 | *.mkv) mpv --fs=yes "$1" & disown;;
	*.png | *.jpeg | *.jpg) $IMAGE_VIEWER "$1" & disown;;
	*.pdf) zathura "$1" & disown;;
	*) case "$(mimetype '$1')" in
		text/* ) $EDITOR "$1";;
		*) mimeopen "$1";;
	esac
esac
