#!/bin/sh

case "$1" in
	*.md) pandoc -f markdown -t html -o "$1.pdf" && rename ".md" "" "$1.pdf";;
	*.ms) groff -k -ms "$1" >> "$1.pdf" && rename ".ms" "" "$1.pdf";;
	*.c) gcc "$1" && mv a.out "$1";;
esac
