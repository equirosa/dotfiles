#!/bin/sh
case "$1" in
	http*) exec youtube-dl -o '~/Videos/watchlist/%(channel)s-%(id)s-%(title)s.%(ext)s' "$1";;
	add ) exec mv "$2" "$HOME/Videos/watchlist/" ;;
	"") exec mpv "$HOME/Videos/watchlist/";;
	* ) exec mv "$1" "$HOME/Videos/watchlist/" ;;
esac
