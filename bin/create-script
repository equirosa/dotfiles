#!/usr/bin/env sh

if [ "$1" = "" ]; then
	notify-send "⚠ERROR: Must specify name of file.⚠" -t 5000;
else
	echo "#!/usr/bin/env sh" >> "$HOME/bin/$1" && chmod +x "$HOME/bin/$1" && $EDITOR "$HOME/bin/$1";
fi
